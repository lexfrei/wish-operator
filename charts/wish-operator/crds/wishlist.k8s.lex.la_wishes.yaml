---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: wishes.wishlist.k8s.lex.la
spec:
  group: wishlist.k8s.lex.la
  names:
    kind: Wish
    listKind: WishList
    plural: wishes
    singular: wish
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Wish is the Schema for the wishes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Wish
            properties:
              contextTags:
                description: ContextTags describe occasions (e.g., "birthday", "christmas").
                items:
                  type: string
                type: array
              description:
                description: Description explains why the user wants this item.
                type: string
              imageURL:
                description: ImageURL is the URL to the product image.
                type: string
              msrp:
                description: MSRP is the price display string (e.g., "₽ 19900").
                type: string
              officialURL:
                description: OfficialURL is the link to the official product page.
                type: string
              priority:
                description: Priority indicates importance (1-5, displayed as stars).
                format: int32
                maximum: 5
                minimum: 0
                type: integer
              purchaseURLs:
                description: PurchaseURLs is a list of links where the item can be
                  purchased.
                items:
                  type: string
                type: array
              quantity:
                default: 1
                description: |-
                  Quantity is the total number of items available for this wish.
                  Defaults to 1 if not specified (backwards compatible).
                format: int32
                minimum: 1
                type: integer
              tags:
                description: Tags are category labels for the wish.
                items:
                  type: string
                type: array
              title:
                description: Title is the name of the desired item.
                minLength: 1
                type: string
              ttl:
                description: TTL defines how long the wish stays active.
                type: string
            required:
            - title
            type: object
          status:
            description: status defines the observed state of Wish
            properties:
              active:
                description: Active indicates if the wish is within its TTL.
                type: boolean
              conditions:
                description: Conditions represent the current state of the Wish resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              reservationExpires:
                description: |-
                  ReservationExpires is when the reservation will expire (1-8 weeks from reservedAt).
                  Deprecated: Use Reservations slice instead.
                format: date-time
                type: string
              reservations:
                description: Reservations is a list of active reservations for this
                  wish.
                items:
                  description: Reservation represents a single reservation of one
                    or more items.
                  properties:
                    createdAt:
                      description: CreatedAt is when this reservation was made.
                      format: date-time
                      type: string
                    expiresAt:
                      description: ExpiresAt is when this reservation will expire.
                      format: date-time
                      type: string
                    quantity:
                      description: Quantity is the number of items reserved in this
                        reservation.
                      format: int32
                      minimum: 1
                      type: integer
                  required:
                  - createdAt
                  - expiresAt
                  - quantity
                  type: object
                type: array
              reserved:
                description: |-
                  Reserved indicates if someone has reserved this wish.
                  Deprecated: Use Reservations slice instead.
                type: boolean
              reservedAt:
                description: |-
                  ReservedAt is the timestamp when the wish was reserved.
                  Deprecated: Use Reservations slice instead.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
